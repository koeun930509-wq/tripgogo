[project]
name = "tripgogo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "python-dotenv",
    "google-genai",
    "flask",
    "flask-cors",
]

[tool.vercel]
entrypoint = "api.recommend:app"
